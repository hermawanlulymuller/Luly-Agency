# Supabase — copy to .env.local for local dev, set in Vercel Dashboard for production
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Injected into pages via /src/js/config/env.js at build/runtime
# Vercel: Project Settings → Environment Variables
